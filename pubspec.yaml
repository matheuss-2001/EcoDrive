name: ecodrive
description: EcoDrive is an Platform which helps user to plan the best charge routes based on theirs car's consumption.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev


version: 1.0.0+1

environment:
  sdk: '>=2.19.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  cupertino_icons: ^1.0.2
  get: ^4.6.5
  url_launcher: ^6.1.7
  geolocator: ^9.0.2
  font_awesome_flutter: ^10.3.0
  get_storage: ^2.0.3
  http: ^0.13.5
  device_info_plus: ^8.0.0
  crypto: ^3.0.2



dev_dependencies:
  flutter_lints: ^2.0.1
  flutter_launcher_icons: ^0.10.0
  flutter_native_splash: ^2.2.9
  flutter_test:
    sdk: flutter




flutter:
  uses-material-design: true









